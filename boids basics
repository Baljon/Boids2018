import numpy as np
import math

width, height = 800, 600
n=50

pos = [width/2.0, height/2.0] + 5*np.random.rand(2*n).reshape(n,2)
angles = 2*math.pi*np.random.rand(n)
vel = np.array(list(zip(np.sin(angles), np.cos(angles))))
